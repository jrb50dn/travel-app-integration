{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon travefy_mapping"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
